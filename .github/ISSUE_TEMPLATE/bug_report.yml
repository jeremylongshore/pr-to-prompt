name: Bug Report
description: Report a bug in pr-to-spec
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: "Thanks for reporting a bug. Please fill out the sections below."

  - type: textarea
    id: description
    attributes:
      label: Description
      description: What happened?
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
      description: What did you expect to happen?
    validations:
      required: true

  - type: textarea
    id: repro
    attributes:
      label: Reproduction steps
      description: How can we reproduce this?
      placeholder: |
        1. Run `pr-to-spec --repo owner/repo --pr 42`
        2. See error...

  - type: input
    id: version
    attributes:
      label: Version
      description: "pr-to-spec version (run `pr-to-spec --version`)"

  - type: dropdown
    id: usage
    attributes:
      label: Usage context
      options:
        - CLI
        - GitHub Action
        - Library
